import React from 'react';

const props = {
    temp: null
};

const unbarSMS = (props) => {

    return (
       <div>
            <h3>Unbar SMS</h3>
       </div>
    );
};

export default unbarSMS;

